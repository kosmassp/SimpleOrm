{
  "name": "limit_only",
  "comment": "Limit without offset: a single bound limit parameter.",
  "entity": "Role",
  "select": { "limit": 3 },
  "expect": { "sqlite": {
    "sql": "select id, role_name, created_at, updated_at from roles limit @c0",
    "parameters": [3] } }
}
