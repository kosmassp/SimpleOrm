{
  "name": "select_all",
  "comment": "No predicates: explicit column list (never *), no WHERE clause.",
  "entity": "Role",
  "select": { },
  "expect": { "sqlite": {
    "sql": "select id, role_name, created_at, updated_at from roles",
    "parameters": [] } }
}
