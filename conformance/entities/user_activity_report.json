{
  "entity": "UserActivityReport",
  "source": {
    "kind": "procedure",
    "name": "user_activity_report",
    "sql": "select u.id as user_id, u.name as user_name, count(t.id) as transaction_count, coalesce(sum(t.amount), 0) as total_amount, max(t.created_at) as last_transaction_at_utc from users u left join transactions t on t.user_id = u.id and t.created_at \u003E= @since group by u.id, u.name",
    "parameters": [
      {
        "name": "since",
        "type": "datetime"
      }
    ]
  },
  "key": {
    "strategy": "none",
    "columns": []
  },
  "columns": [
    {
      "column": "user_id",
      "type": "int64",
      "nullable": false
    },
    {
      "column": "user_name",
      "type": "string",
      "nullable": false
    },
    {
      "column": "transaction_count",
      "type": "int32",
      "nullable": false
    },
    {
      "column": "total_amount",
      "type": "decimal",
      "nullable": false
    },
    {
      "column": "last_transaction_at_utc",
      "type": "datetime",
      "nullable": true
    }
  ]
}
