{
  "entity": "UserTransactionTotal",
  "source": {
    "kind": "view",
    "name": "user_transaction_totals",
    "sql": "select u.id as user_id, u.name as user_name, count(t.id) as transaction_count, coalesce(sum(t.amount), 0) as total_amount, max(t.created_at) as last_transaction_at from users u left join transactions t on t.user_id = u.id group by u.id, u.name"
  },
  "key": {
    "strategy": "natural",
    "columns": [
      "user_id"
    ]
  },
  "columns": [
    {
      "column": "user_id",
      "type": "int64",
      "nullable": false,
      "key": true
    },
    {
      "column": "user_name",
      "type": "string",
      "nullable": false
    },
    {
      "column": "transaction_count",
      "type": "int32",
      "nullable": false
    },
    {
      "column": "total_amount",
      "type": "decimal",
      "nullable": false
    },
    {
      "column": "last_transaction_at",
      "type": "datetime",
      "nullable": true
    }
  ]
}
